<%- include('includes/head.ejs') %>

<body>
  <% if(users.length > 0) { %>
  <h1>Users</h1>
  <ul>
    <% users.forEach(user => { %>
    <li>Username: <%= user.userName %></li>
    <%  }) %>
  </ul>
  <% } else { %>
  <h1>No Users Found!</h1>
  <% } %>
  <%- include('includes/end.ejs') %>